import LoginForm from "@/app/[locale]/login";

const LoginPage = () => {
  return (
    <LoginForm className="absolute -top-42 left-1/2 transform -translate-x-1/2 w-full mt-12 max-w-6xl" />
  );
};

export default LoginPage;
