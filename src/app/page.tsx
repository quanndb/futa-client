import TicketBookingPage from "@/components/TicketBooking";

const index = () => {
  return (
    <div className="absolute max-w-6xl -top-40 left-1/2 transform -translate-x-1/2 w-full mt-12 ">
      <TicketBookingPage />
    </div>
  );
};

export default index;
